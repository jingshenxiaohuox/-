# ISO七层模型
            物理层：只管传输0和1
            数据链路层：不认识IP，只认识MAC帧。（通过MAC地址找到下一跳）
            网络层：负责跨网络协议，在网络中找到要通信的对象
            传输层：从网络层结果包，传给指定的端口
            会话层：建立和管理链接（维护文件描述符，socket连接）
            表示层：负责数据格式的转换，加密解密（大端序到小端序，小端序到大端序）
            应用层：指定业务数据的解析规则

# 